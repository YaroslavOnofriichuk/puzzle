import type { Matrix, Figure } from "../types";

export const canSetFigure = (matrix: Matrix, figure: Figure, position: number[]): boolean => {
    console.log("matrix", matrix)
    console.log("figure", figure)
    console.log("position", position)

    return false
};